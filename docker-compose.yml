
version: '3.8'
services:
  linkedin-api:
    build: .
    restart: always
    ports:
      - "3020:3020"
    volumes:
      - sessions_data:/data/sessions
volumes:
  sessions_data:
    driver: local
